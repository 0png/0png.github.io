export interface SiteConfig {
  name: string
  headline: string
  bio: string
  githubUrl: string
  year: number
}

export const siteConfig: SiteConfig = {
  name: '0PNG',
  headline: 'I build tools that actually work.',
  bio: 'Secondary school student, AI developer, and long-distance runner. I build useful things.',
  githubUrl: 'https://github.com/0png',
  year: 2026,
}
